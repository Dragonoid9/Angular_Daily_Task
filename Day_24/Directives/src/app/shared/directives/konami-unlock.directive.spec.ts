import { KonamiUnlockDirective } from './konami-unlock.directive';

describe('KonamiUnlockDirective', () => {
  it('should create an instance', () => {
    const directive = new KonamiUnlockDirective();
    expect(directive).toBeTruthy();
  });
});
